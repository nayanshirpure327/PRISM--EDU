import { LoadingScreen } from '@/components/ui/loading-screen';

export default function AdminLoading() {
  return <LoadingScreen message="Loading Admin Management Section..." fullScreen={true} />;
}

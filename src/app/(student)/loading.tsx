import { LoadingScreen } from '@/components/ui/loading-screen';

export default function StudentLoading() {
  return <LoadingScreen message="Loading Student Portal & Learning Environment..." fullScreen={true} />;
}

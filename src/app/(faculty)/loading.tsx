import { LoadingScreen } from '@/components/ui/loading-screen';

export default function FacultyLoading() {
  return <LoadingScreen message="Loading Faculty Portal & Analytics..." fullScreen={true} />;
}
